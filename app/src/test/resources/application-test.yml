spring:
  datasource:
    url: jdbc:h2:mem:compute;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

security:
  jwt:
    secret: test-secret-that-is-long-enough-for-hs256

storage:
  signing-secret: test-storage-secret-long-enough-to-sign
