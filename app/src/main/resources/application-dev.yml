# Development only. Production must supply JWT_SECRET, which has no default so that a missing
# value fails startup rather than turning every request into a 401.
security:
  jwt:
    secret: dev-only-secret-not-for-production-32b

storage:
  signing-secret: dev-only-storage-secret-not-for-prod
